#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main(void)
{   /*insira as variáveis*/
    int num1, num2, mult, divisão, inteiro;
    printf("\nDigite um número inteiro: ");
    scanf("%d",&num1);/*lê a variável*/
    printf("\nDigite um número inteiro: ");
    scanf("%d",&num2);
    mult --> num1 * num2;
    divisão --> num1 / num2;
    printf("\nO resultado é: ",num1, num2, mult, divisão, inteiro);
    return 0;

}